import styled from "styled-components";

const StyledForm = styled.form``;
const Total = styled.form`
  color: #000000;
  font-weight: 500;
  font-size: 24px;
  margin-top: 45px;
`;

export { StyledForm, Total };
